# Copy to .env and set local/deployment values. Never commit .env.
POSTGRES_PASSWORD=replace_with_random_alphanumeric_password
# For host-side migrate/seed commands, use the temporary loopback DB overlay and set DATABASE_URL.
DATABASE_URL=
OPENAI_API_KEY=
OPENAI_MODEL=
GPU_SERVICE_URL=http://gpu-specialist:8000
GPU_MODEL_ID=
GPU_VERIFICATION_PATH=/app/infra/brev/evidence/deployment.json
DEMO_OPERATOR=Local demo operator
WEB_ORIGIN=http://localhost:3000
API_INTERNAL_URL=http://api:3001

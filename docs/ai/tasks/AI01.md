# AI01 — AI package and runtime configuration boundary

## Goal

AI package and runtime configuration boundary. Wave 0; estimated active effort 20 minutes (download waits may overlap other work). Follow [execution conventions](../../execution-conventions.md); commands below are implementation targets, not already-existing commands.

## Owner

Ali — ai lane. Delegation never changes single-writer ownership.

## Recommended model

GPT-6 Sol Medium. Lane orchestrator: GPT-6 Sol High. Difficult bug/architecture conflict: GPT-6 Sol High; only after a serious evidenced attempt, GPT-6 Astra High. Simple bounded workers and README/tests/fixtures/small fixes: GPT-6 Luna High; normal coding workers: GPT-6 Sol Medium. No default Astra coding.

## Exact write scope

- `packages/ai/package.json`
- `packages/ai/tsconfig.json`
- `packages/ai/src/index.ts`
- `packages/ai/src/config.ts`
- `packages/ai/test/config.test.ts`

## Forbidden paths

apps/api/**; packages/db/**; apps/web/**; packages/contracts/**; infra/**; root manifests/lockfile/Compose/env; README.md; docs/**. Allowed paths are only the explicit scope above; the task document itself is read-only to the worker. Read-only imports/inspection of dependencies allowed. Dependencies are not write permission. No real credentials in tracked files.

## Dependencies

B01, B02

A `:configured` dependency means interface/tests ready; `:executed` additionally requires real GPU evidence. A task with a later live check may develop against a truthful unavailable boundary first, but cannot claim complete early.

## Relevant contracts

A01/P01/P02; environment policy; RuntimeStatus

## Implementation requirements

Create @atlas/ai package and build/typecheck scripts using Agents SDK plus shared contracts; request shared lockfile updates through Ivan. Config validates server-only OPENAI_API_KEY/MODEL, GPU_SERVICE_URL/MODEL_ID/VERIFICATION_PATH. Missing OpenAI access fails live smoke without crashing dataset API; GPU unavailable is representable, no fake client. Fixed endpoint from env allowlist, no model-controlled URLs. Export A01 via index once AI04 implements it, never an executable stub returning success.
Define internal dependency injection for tests: model transport, fetch, clock and read-only verification record. No DB import or approval tool. No hosted NVIDIA configuration or registry key in product inference client. No application bootstrap must await model downloads.

## Acceptance criteria

Package types/build/config tests pass; absent GPU gives truthful configuration state only. No configured adapter is counted as actual GPU runtime.

## Exact verification command/check

Run at repository root in the configured development environment unless the check says Brev. Script/file creation belongs to this task unless [execution conventions](../../execution-conventions.md) assigns its producer. Missing script is a failed/incomplete task, never a pass.

```text
pnpm --filter @atlas/ai typecheck
pnpm --filter @atlas/ai exec tsx --test test/config.test.ts
```

## Integration impact

Unblocks disjoint OpenAI worker and GPU client worker; config/index modifications later serialize through Ali.

## Safe to delegate to a subagent

Yes, with an exclusive scope lease and the model below. Parent reviews and owns integration.

## Ready-to-run worker prompt

Read AGENTS.md, MODEL-POLICY.md, docs/execution-conventions.md and docs/ai/tasks/AI01.md. Execute only AI01 for Ali. Verify dependencies including configured/executed qualifiers. Use GPT-6 Sol Medium. Write only: packages/ai/package.json; packages/ai/tsconfig.json; packages/ai/src/index.ts; packages/ai/src/config.ts; packages/ai/test/config.test.ts. Do not change contracts, shared files or another worker's files outside this scope. Implement the packet requirements, run every listed check, and return changed files, commands/results, actual-versus-configured runtime evidence and blockers. No fabricated OpenAI/GPU results, no secrets, no unrelated refactors. Do not commit/push unless the lane orchestrator explicitly assigns it. Stop with a precise mismatch if scope or contract blocks you.

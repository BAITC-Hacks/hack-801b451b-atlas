# B15 — Freeze audit final README and submission

## Goal

Freeze audit final README and submission. Wave 4; estimated active effort 20 minutes (download waits may overlap other work). Follow [execution conventions](../../execution-conventions.md); commands below are implementation targets, not already-existing commands.

## Owner

Ivan — backend lane. Delegation never changes single-writer ownership.

## Recommended model

GPT-6 Luna High. Lane orchestrator: GPT-6 Sol High. Difficult bug/architecture conflict: GPT-6 Sol High; only after a serious evidenced attempt, GPT-6 Astra High. Simple bounded workers and README/tests/fixtures/small fixes: GPT-6 Luna High; normal coding workers: GPT-6 Sol Medium. No default Astra coding.

## Exact write scope

- `README.md`
- `docs/review/final.md`
- `docs/final-checklist.md`
- `compose.yaml`
- `.env.example`
- `pnpm-lock.yaml`
- `infra/brev/**`

## Forbidden paths

apps/web/**; packages/ai/**; every shared/infra/API/DB path not explicitly allowed below. Allowed paths are only the explicit scope above; the task document itself is read-only to the worker. Read-only imports/inspection of dependencies allowed. Dependencies are not write permission. No real credentials in tracked files.

## Dependencies

B11, B14, FRT08, AI08

A `:configured` dependency means interface/tests ready; `:executed` additionally requires real GPU evidence. A task with a later live check may develop against a truthful unavailable boundary first, but cannot claim complete early.

## Relevant contracts

Final-checklist; Sentinel FULL; C14 and runtime verification

## Implementation requirements

No features. Run final checklist and Sentinel FULL; route P0/P1 code findings to lane owners, never fix their files silently. Rerun affected gate after fixes. Final README includes exact clean/Brev setup, model/image and conditional credentials, smoke reproduction, cost/stop instructions, real degraded behavior, privacy/1C limitations and all disclosures. Redact secrets, retain non-sensitive reproducible evidence. Feature-freeze time remains240min even if a mandatory check is still red; report it as a blocker rather than pretending passed. Commit only owned changes, coordinate final lane commits, pull/rebase cleanly, verify and push. No empty progress commits.

## Acceptance criteria

All runtime/live checks rerun against final revision or explicitly fail; no unsupported README claim. Submission status includes true external gaps and final commit ID; successful push verified. Nothing implies configuration alone is GPU execution.

## Exact verification command/check

Run at repository root in the configured development environment unless the check says Brev. Script/file creation belongs to this task unless [execution conventions](../../execution-conventions.md) assigns its producer. Missing script is a failed/incomplete task, never a pass.

```text
git diff --check
pnpm -r --if-present typecheck
pnpm -r --if-present build
pnpm --filter @atlas/api exec tsx scripts/combined-flow.ts
Run every box in docs/final-checklist.md and Sentinel FULL.
git status --short
git log -1 --oneline
```

## Integration impact

Final gate owner Ivan; documentation worker may assist only during exclusive README window.

## Safe to delegate to a subagent

No for the integration/ownership decision. Keep with lane orchestrator; implementation fragments require a separately bounded disjoint lease.

## Orchestrator execution prompt

Read AGENTS.md, MODEL-POLICY.md, docs/execution-conventions.md and docs/backend/tasks/B15.md. Execute only B15 for Ivan. Verify dependencies including configured/executed qualifiers. Use GPT-6 Luna High. Write only: README.md; docs/review/final.md; docs/final-checklist.md; compose.yaml; .env.example; pnpm-lock.yaml; infra/brev/**. Do not change contracts, shared files or another worker's files outside this scope. Implement the packet requirements, run every listed check, and return changed files, commands/results, actual-versus-configured runtime evidence and blockers. No fabricated OpenAI/GPU results, no secrets, no unrelated refactors. Do not commit/push unless the lane orchestrator explicitly assigns it. Stop with a precise mismatch if scope or contract blocks you. Keep the live gate/ownership decision with the lane orchestrator.

# B01 — Root workspace and base Compose

## Goal

Root workspace and base Compose. Wave 0; estimated active effort 20 minutes (download waits may overlap other work). Follow [execution conventions](../../execution-conventions.md); commands below are implementation targets, not already-existing commands.

## Owner

Ivan — backend lane. Delegation never changes single-writer ownership.

## Recommended model

GPT-6 Sol Medium. Lane orchestrator: GPT-6 Sol High. Difficult bug/architecture conflict: GPT-6 Sol High; only after a serious evidenced attempt, GPT-6 Astra High. Simple bounded workers and README/tests/fixtures/small fixes: GPT-6 Luna High; normal coding workers: GPT-6 Sol Medium. No default Astra coding.

## Exact write scope

- `package.json`
- `pnpm-workspace.yaml`
- `pnpm-lock.yaml`
- `tsconfig.base.json`
- `.gitignore`
- `.dockerignore`
- `.env.example`
- `compose.yaml`
- `Dockerfile.api`
- `Dockerfile.web`

## Forbidden paths

apps/web/**; packages/ai/**; every shared/infra/API/DB path not explicitly allowed below. Allowed paths are only the explicit scope above; the task document itself is read-only to the worker. Read-only imports/inspection of dependencies allowed. Dependencies are not write permission. No real credentials in tracked files.

## Dependencies

None; initial repository planning files are available.

A `:configured` dependency means interface/tests ready; `:executed` additionally requires real GPU evidence. A task with a later live check may develop against a truthful unavailable boundary first, but cannot claim complete early.

## Relevant contracts

AGENTS.md; MODEL-POLICY.md; docs/product-spec.md architecture; docs/execution-conventions.md

## Implementation requirements

Create pnpm workspace for apps/* and packages/*; root dev-only TypeScript/tsx, compatible Node/pnpm versions pinned after checking installed support. Root typecheck/build scripts use pnpm -r --if-present, never claim a missing package built. Coordinate package dependency requests from FRT01/AI01/B02/B03/B04, then Ivan alone refreshes lockfile; this is an ongoing serialized handoff, not worker installs racing.
Base Compose defines web/api/db, web loopback3000, internal api3001 and PostgreSQL5432; no public API/db ports. API may run without GPU; no hard healthy-GPU dependency. Docker builds consume the shared lockfile and workspace packages. Env fields exactly follow contracts: DATABASE_URL, OPENAI_API_KEY, OPENAI_MODEL, GPU_SERVICE_URL=http://gpu-specialist:8000, GPU_MODEL_ID, GPU_VERIFICATION_PATH, DEMO_OPERATOR, WEB_ORIGIN, API_INTERNAL_URL. Do not add registry credentials unless B06 proves them necessary. Private secrets ignored; do not print expanded Compose config with populated secrets. Skeleton images may wait for lane source, but DB must run now.

## Acceptance criteria

Configured: workspace discovery and redacted Compose validation succeed; DB health succeeds. Actual GPU execution is not claimed here. All later package script producers are listed in execution-conventions.md.

## Exact verification command/check

Run at repository root in the configured development environment unless the check says Brev. Script/file creation belongs to this task unless [execution conventions](../../execution-conventions.md) assigns its producer. Missing script is a failed/incomplete task, never a pass.

```text
pnpm install
pnpm -r list --depth -1
docker compose config --quiet
docker compose up -d db
docker compose exec -T db pg_isready -U atlas -d atlas
```

## Integration impact

Unblocks dependency installation and B06; parent serializes root changes with B11. DB user/database atlas; password comes from env.

## Safe to delegate to a subagent

No for the integration/ownership decision. Keep with lane orchestrator; implementation fragments require a separately bounded disjoint lease.

## Orchestrator execution prompt

Read AGENTS.md, MODEL-POLICY.md, docs/execution-conventions.md and docs/backend/tasks/B01.md. Execute only B01 for Ivan. Verify dependencies including configured/executed qualifiers. Use GPT-6 Sol Medium. Write only: package.json; pnpm-workspace.yaml; pnpm-lock.yaml; tsconfig.base.json; .gitignore; .dockerignore; .env.example; compose.yaml; Dockerfile.api; Dockerfile.web. Do not change contracts, shared files or another worker's files outside this scope. Implement the packet requirements, run every listed check, and return changed files, commands/results, actual-versus-configured runtime evidence and blockers. No fabricated OpenAI/GPU results, no secrets, no unrelated refactors. Do not commit/push unless the lane orchestrator explicitly assigns it. Stop with a precise mismatch if scope or contract blocks you. Keep the live gate/ownership decision with the lane orchestrator.

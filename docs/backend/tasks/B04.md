# B04 — Fastify health and normalized import

## Goal

Fastify health and normalized import. Wave 0; estimated active effort 25 minutes (download waits may overlap other work). Follow [execution conventions](../../execution-conventions.md); commands below are implementation targets, not already-existing commands.

## Owner

Ivan — backend lane. Delegation never changes single-writer ownership.

## Recommended model

GPT-6 Sol Medium. Lane orchestrator: GPT-6 Sol High. Difficult bug/architecture conflict: GPT-6 Sol High; only after a serious evidenced attempt, GPT-6 Astra High. Simple bounded workers and README/tests/fixtures/small fixes: GPT-6 Luna High; normal coding workers: GPT-6 Sol Medium. No default Astra coding.

## Exact write scope

- `apps/api/package.json`
- `apps/api/tsconfig.json`
- `apps/api/src/app.ts`
- `apps/api/src/server.ts`
- `apps/api/src/config.ts`
- `apps/api/src/routes.ts`
- `apps/api/src/datasets/**`
- `apps/api/test/import.test.ts`

## Forbidden paths

apps/web/**; packages/ai/**; every shared/infra/API/DB path not explicitly allowed below. Allowed paths are only the explicit scope above; the task document itself is read-only to the worker. Read-only imports/inspection of dependencies allowed. Dependencies are not write permission. No real credentials in tracked files.

## Dependencies

B03

A `:configured` dependency means interface/tests ready; `:executed` additionally requires real GPU evidence. A task with a later live check may develop against a truthful unavailable boundary first, but cannot claim complete early.

## Relevant contracts

H01/H02/H08; Error; DatasetInput; sources/privacy rules

## Implementation requirements

Create @atlas/api bootstrap, typecheck/build/start scripts and app factory. Bind within container on3001; Compose controls public reachability. Implement health with actual DB check, POST datasets atomic validation/hash/save and GET summaries. Strict 5MiB/row limits, duplicate/FK/date/unit rejection, privacy confirmation, opaque token format, provenance and fixed asOf. No remote file fetching or unstructured Excel import. Error envelope includes requestId and safe issue paths/codes. Normalized source name is data, not prompt instructions. Basic origin/content-type validation begins now; B10 hardens it. H01/H02 available without GPU/OpenAI keys; startup supports degraded runtime readiness without faking successful config.
No inventory calculation or direct model client here. Provide package-level script slots for build/typecheck, while smoke commands are owned by their later tasks.

## Acceptance criteria

G-API: POST valid synthetic data=>201, list contains it, malformed data=>400/413, health reflects DB. No unimplemented run route returns fabricated success.

## Exact verification command/check

Run at repository root in the configured development environment unless the check says Brev. Script/file creation belongs to this task unless [execution conventions](../../execution-conventions.md) assigns its producer. Missing script is a failed/incomplete task, never a pass.

```text
pnpm --filter @atlas/api typecheck
pnpm --filter @atlas/api exec tsx --test test/import.test.ts
pnpm --filter @atlas/api build
```

## Integration impact

FRT03 can use dataset controls; src/routes.ts is later handed sequentially to B07 then B08.

## Safe to delegate to a subagent

Yes, with an exclusive scope lease and the model below. Parent reviews and owns integration.

## Ready-to-run worker prompt

Read AGENTS.md, MODEL-POLICY.md, docs/execution-conventions.md and docs/backend/tasks/B04.md. Execute only B04 for Ivan. Verify dependencies including configured/executed qualifiers. Use GPT-6 Sol Medium. Write only: apps/api/package.json; apps/api/tsconfig.json; apps/api/src/app.ts; apps/api/src/server.ts; apps/api/src/config.ts; apps/api/src/routes.ts; apps/api/src/datasets/**; apps/api/test/import.test.ts. Do not change contracts, shared files or another worker's files outside this scope. Implement the packet requirements, run every listed check, and return changed files, commands/results, actual-versus-configured runtime evidence and blockers. No fabricated OpenAI/GPU results, no secrets, no unrelated refactors. Do not commit/push unless the lane orchestrator explicitly assigns it. Stop with a precise mismatch if scope or contract blocks you.

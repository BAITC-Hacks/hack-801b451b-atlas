# B13 — README after first live milestone

## Goal

README after first live milestone. Wave 1; estimated active effort 5 minutes (download waits may overlap other work). Follow [execution conventions](../../execution-conventions.md); commands below are implementation targets, not already-existing commands.

## Owner

Ivan — backend lane. Delegation never changes single-writer ownership.

## Recommended model

GPT-6 Luna High. Lane orchestrator: GPT-6 Sol High. Difficult bug/architecture conflict: GPT-6 Sol High; only after a serious evidenced attempt, GPT-6 Astra High. Simple bounded workers and README/tests/fixtures/small fixes: GPT-6 Luna High; normal coding workers: GPT-6 Sol Medium. No default Astra coding.

## Exact write scope

- `README.md`

## Forbidden paths

apps/web/**; packages/ai/**; every shared/infra/API/DB path not explicitly allowed below. Allowed paths are only the explicit scope above; the task document itself is read-only to the worker. Read-only imports/inspection of dependencies allowed. Dependencies are not write permission. No real credentials in tracked files.

## Dependencies

B12

A `:configured` dependency means interface/tests ready; `:executed` additionally requires real GPU evidence. A task with a later live check may develop against a truthful unavailable boundary first, but cannot claim complete early.

## Relevant contracts

C14; actual docs/review/combined.md evidence; MODEL-POLICY.md

## Implementation requirements

Spend<=5min replacing only proven README placeholders with actual start commands, OpenAI role, Brev runtime/model/endpoint, GPU smoke command and current evidence link. State configured versus verified and actual remaining limitations. Keep case/math links, synthetic-data/harness/model/image disclosures. Do not promise unverified accounting compatibility or hide partial blockers.

## Acceptance criteria

README contains a reproducible first-slice command trail and actual runtime evidence; every success claim references a performed check.

## Exact verification command/check

Run at repository root in the configured development environment unless the check says Brev. Script/file creation belongs to this task unless [execution conventions](../../execution-conventions.md) assigns its producer. Missing script is a failed/incomplete task, never a pass.

```text
git diff --check
Manual: compare README claims/commands against docs/review/combined.md and B06 deployment.md.
```

## Integration impact

Continuous cheap documentation; only Ivan or his exclusive README worker writes; serialize with B14/B15.

## Safe to delegate to a subagent

Yes, with an exclusive scope lease and the model below. Parent reviews and owns integration.

## Ready-to-run worker prompt

Read AGENTS.md, MODEL-POLICY.md, docs/execution-conventions.md and docs/backend/tasks/B13.md. Execute only B13 for Ivan. Verify dependencies including configured/executed qualifiers. Use GPT-6 Luna High. Write only: README.md. Do not change contracts, shared files or another worker's files outside this scope. Implement the packet requirements, run every listed check, and return changed files, commands/results, actual-versus-configured runtime evidence and blockers. No fabricated OpenAI/GPU results, no secrets, no unrelated refactors. Do not commit/push unless the lane orchestrator explicitly assigns it. Stop with a precise mismatch if scope or contract blocks you.

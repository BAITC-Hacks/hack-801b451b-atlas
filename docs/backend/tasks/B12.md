# B12 — Combined OpenAI GPU user-flow gate

## Goal

Combined OpenAI GPU user-flow gate. Wave 1; estimated active effort 15 minutes (download waits may overlap other work). Follow [execution conventions](../../execution-conventions.md); commands below are implementation targets, not already-existing commands.

## Owner

Ivan — backend lane. Delegation never changes single-writer ownership.

## Recommended model

GPT-6 Sol High. Lane orchestrator: GPT-6 Sol High. Difficult bug/architecture conflict: GPT-6 Sol High; only after a serious evidenced attempt, GPT-6 Astra High. Simple bounded workers and README/tests/fixtures/small fixes: GPT-6 Luna High; normal coding workers: GPT-6 Sol Medium. No default Astra coding.

## Exact write scope

- `apps/api/scripts/combined-flow.ts`
- `docs/review/combined.md`

## Forbidden paths

apps/web/**; packages/ai/**; every shared/infra/API/DB path not explicitly allowed below. Allowed paths are only the explicit scope above; the task document itself is read-only to the worker. Read-only imports/inspection of dependencies allowed. Dependencies are not write permission. No real credentials in tracked files.

## Dependencies

B06, B08, AI05, FRT04

A `:configured` dependency means interface/tests ready; `:executed` additionally requires real GPU evidence. A task with a later live check may develop against a truthful unavailable boundary first, but cannot claim complete early.

## Relevant contracts

H01–H07; A01/P01/P02/G01; GpuEvidence; main scenario

## Implementation requirements

Create/run combined-flow script against real API to inspect seed, run agent, assert openai and brev_gpu success, current GPU evidence and matching calculation ID, then edit/approve/export/reload. Compare current specialist calculation to T02(null) for the same dataset/scope as a clearly labeled deterministic counterfactual; do not relabel counterfactual as a second live result. Match eventActions and quantity/review difference for a real borderline classification. Script must fail if no actual influence can be shown; do not force model output or weaken threshold.
Coordinate Beka's visible click-through from FRT04 and Ivan's same-run GPU telemetry in B06. Save redacted run ID, dataset hash, model/deployment identity, actual inference ID/time, action/metrics difference and UI approval/export evidence. Run Sentinel INTEGRATION once at this convergence; report findings, don't edit outside scope.

## Acceptance criteria

G-COMBINED: real UI→API→OpenAI→Brev GPU→calculator→persist→review/edit/approve→CSV in RU or EN; actual GPU computation and material effect, plus current provenance. W1 does not close on configured/mocked/CPU-only proof.

## Exact verification command/check

Run at repository root in the configured development environment unless the check says Brev. Script/file creation belongs to this task unless [execution conventions](../../execution-conventions.md) assigns its producer. Missing script is a failed/incomplete task, never a pass.

```text
pnpm --filter @atlas/api exec tsx scripts/combined-flow.ts
Manual: open http://localhost:3000, select seed/warehouse, Calculate, inspect GPU event, edit quantity/reason, acknowledge, approve, download, reload.
Run docs/review/sentinel.md in INTEGRATION mode against this run.
```

## Integration impact

Closes W1; joins parallel runtime and product paths; orchestrator retains live gate.

## Safe to delegate to a subagent

No for the integration/ownership decision. Keep with lane orchestrator; implementation fragments require a separately bounded disjoint lease.

## Orchestrator execution prompt

Read AGENTS.md, MODEL-POLICY.md, docs/execution-conventions.md and docs/backend/tasks/B12.md. Execute only B12 for Ivan. Verify dependencies including configured/executed qualifiers. Use GPT-6 Sol High. Write only: apps/api/scripts/combined-flow.ts; docs/review/combined.md. Do not change contracts, shared files or another worker's files outside this scope. Implement the packet requirements, run every listed check, and return changed files, commands/results, actual-versus-configured runtime evidence and blockers. No fabricated OpenAI/GPU results, no secrets, no unrelated refactors. Do not commit/push unless the lane orchestrator explicitly assigns it. Stop with a precise mismatch if scope or contract blocks you. Keep the live gate/ownership decision with the lane orchestrator.

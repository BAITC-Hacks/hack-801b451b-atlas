# B05 — Deterministic demand inspection and replenishment

## Goal

Deterministic demand inspection and replenishment. Wave 1; estimated active effort 30 minutes (download waits may overlap other work). Follow [execution conventions](../../execution-conventions.md); commands below are implementation targets, not already-existing commands.

## Owner

Ivan — backend lane. Delegation never changes single-writer ownership.

## Recommended model

GPT-6 Sol Medium. Lane orchestrator: GPT-6 Sol High. Difficult bug/architecture conflict: GPT-6 Sol High; only after a serious evidenced attempt, GPT-6 Astra High. Simple bounded workers and README/tests/fixtures/small fixes: GPT-6 Luna High; normal coding workers: GPT-6 Sol Medium. No default Astra coding.

## Exact write scope

- `apps/api/src/calculation/**`
- `apps/api/src/tools/**`
- `apps/api/test/calculation.test.ts`

## Forbidden paths

apps/web/**; packages/ai/**; every shared/infra/API/DB path not explicitly allowed below. Allowed paths are only the explicit scope above; the task document itself is read-only to the worker. Read-only imports/inspection of dependencies allowed. Dependencies are not write permission. No real credentials in tracked files.

## Dependencies

B02, B03

A `:configured` dependency means interface/tests ready; `:executed` additionally requires real GPU evidence. A task with a later live check may develop against a truthful unavailable boundary first, but cannot claim complete early.

## Relevant contracts

T01/T02; Calculation/Candidate; product-spec replenishment-v1 steps1–7; C01–C04

## Implementation requirements

Implement the entire frozen formula now, without placeholder all-history averages. Customer/SKU/day aggregation,90-day median/MAD candidates, hard exclusions, recurrence safeguards, typed GPU report policy, daily zeros/stockout imputation, month seasonal factors, weekly sustained-trend gate, residual category uplift, horizon/inbound/stock subtraction, final ceil and urgency. Missing critical stock errors; sparse history warnings/zero behavior exactly per spec. Candidate overflow fails, never truncates. Source contributions and rawSalesQty counterfactual remain auditable.
Publish src/tools/index.ts createDemandTools({datasetId,warehouseId,categoryId?,runId}) returning {scopeId,tools,lookupCalculation} as fixed in execution-conventions.md. Tools close over one validated scope, honor AbortSignal, cache request-local calculation ID; no SQL or arbitrary IDs accepted from AI. calculate can inspect internally if OpenAI never reached inspect. Output raw numbers originate solely here; post-model confidence is not trusted without semantic guards. Dates use calendar arithmetic independent of local timezone. Float rounding must preserve mathematical integer results rather than spuriously adding one due to machine epsilon; test exact80/60/94 fixtures.

## Acceptance criteria

Exact math and required seasonal/growth/stockout/outlier fixtures pass. T02(null) produces provisional exclusions with warnings; configured GPU reports in unit tests are explicitly stubs and do not prove inference.

## Exact verification command/check

Run at repository root in the configured development environment unless the check says Brev. Script/file creation belongs to this task unless [execution conventions](../../execution-conventions.md) assigns its producer. Missing script is a failed/incomplete task, never a pass.

```text
pnpm --filter @atlas/api exec tsx --test test/calculation.test.ts
pnpm --filter @atlas/api typecheck
```

## Integration impact

Unblocks B07 and AI05; B09 broadens proof fixtures after this implementation, not duplicate formula code.

## Safe to delegate to a subagent

Yes, with an exclusive scope lease and the model below. Parent reviews and owns integration.

## Ready-to-run worker prompt

Read AGENTS.md, MODEL-POLICY.md, docs/execution-conventions.md and docs/backend/tasks/B05.md. Execute only B05 for Ivan. Verify dependencies including configured/executed qualifiers. Use GPT-6 Sol Medium. Write only: apps/api/src/calculation/**; apps/api/src/tools/**; apps/api/test/calculation.test.ts. Do not change contracts, shared files or another worker's files outside this scope. Implement the packet requirements, run every listed check, and return changed files, commands/results, actual-versus-configured runtime evidence and blockers. No fabricated OpenAI/GPU results, no secrets, no unrelated refactors. Do not commit/push unless the lane orchestrator explicitly assigns it. Stop with a precise mismatch if scope or contract blocks you.

# B08 — Revision-safe edit approval and export

## Goal

Revision-safe edit approval and export. Wave 1; estimated active effort 25 minutes (download waits may overlap other work). Follow [execution conventions](../../execution-conventions.md); commands below are implementation targets, not already-existing commands.

## Owner

Ivan — backend lane. Delegation never changes single-writer ownership.

## Recommended model

GPT-6 Sol Medium. Lane orchestrator: GPT-6 Sol High. Difficult bug/architecture conflict: GPT-6 Sol High; only after a serious evidenced attempt, GPT-6 Astra High. Simple bounded workers and README/tests/fixtures/small fixes: GPT-6 Luna High; normal coding workers: GPT-6 Sol Medium. No default Astra coding.

## Exact write scope

- `apps/api/src/orders/**`
- `apps/api/src/routes.ts`
- `apps/api/test/orders.test.ts`

## Forbidden paths

apps/web/**; packages/ai/**; every shared/infra/API/DB path not explicitly allowed below. Allowed paths are only the explicit scope above; the task document itself is read-only to the worker. Read-only imports/inspection of dependencies allowed. Dependencies are not write permission. No real credentials in tracked files.

## Dependencies

B07

A `:configured` dependency means interface/tests ready; `:executed` additionally requires real GPU evidence. A task with a later live check may develop against a truthful unavailable boundary first, but cannot claim complete early.

## Relevant contracts

H05/H06/H07; C05/C06/C09/C11/C13

## Implementation requirements

Implement draft quantity edits with override reasons and optimistic revision; approval with confirm true and warning acknowledgment; server operator/time/audit, approved immutable. Each mutation transaction checks current revision. Download approved positive final quantities only; rejected before approval and on stale revision. Exact BOM/semicolon/CRLF/quoted headers from API contract; formula injection escaping and localized RU/EN explanations with immutable recommendation plus edited quantity. Empty order header-only. Supplier transmission remains absent. Record accounting CSV as proposed until partner sample accepts mapping, never call it a verified 1C integration.

## Acceptance criteria

Edits preserve recommended quantity, approval survives reload, stale write409, warnings without acknowledgment422, draft export409. RU/EN CSV quantities match approved revision; no network call to supplier.

## Exact verification command/check

Run at repository root in the configured development environment unless the check says Brev. Script/file creation belongs to this task unless [execution conventions](../../execution-conventions.md) assigns its producer. Missing script is a failed/incomplete task, never a pass.

```text
pnpm --filter @atlas/api exec tsx --test test/orders.test.ts
pnpm --filter @atlas/api typecheck
```

## Integration impact

FRT04 and B12 close approved user flow; serialize routes.ts after B07.

## Safe to delegate to a subagent

Yes, with an exclusive scope lease and the model below. Parent reviews and owns integration.

## Ready-to-run worker prompt

Read AGENTS.md, MODEL-POLICY.md, docs/execution-conventions.md and docs/backend/tasks/B08.md. Execute only B08 for Ivan. Verify dependencies including configured/executed qualifiers. Use GPT-6 Sol Medium. Write only: apps/api/src/orders/**; apps/api/src/routes.ts; apps/api/test/orders.test.ts. Do not change contracts, shared files or another worker's files outside this scope. Implement the packet requirements, run every listed check, and return changed files, commands/results, actual-versus-configured runtime evidence and blockers. No fabricated OpenAI/GPU results, no secrets, no unrelated refactors. Do not commit/push unless the lane orchestrator explicitly assigns it. Stop with a precise mismatch if scope or contract blocks you.

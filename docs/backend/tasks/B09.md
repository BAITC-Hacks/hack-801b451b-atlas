# B09 — Case fixtures and source sensitivity proof

## Goal

Case fixtures and source sensitivity proof. Wave 2; estimated active effort 25 minutes (download waits may overlap other work). Follow [execution conventions](../../execution-conventions.md); commands below are implementation targets, not already-existing commands.

## Owner

Ivan — backend lane. Delegation never changes single-writer ownership.

## Recommended model

GPT-6 Luna High. Lane orchestrator: GPT-6 Sol High. Difficult bug/architecture conflict: GPT-6 Sol High; only after a serious evidenced attempt, GPT-6 Astra High. Simple bounded workers and README/tests/fixtures/small fixes: GPT-6 Luna High; normal coding workers: GPT-6 Sol Medium. No default Astra coding.

## Exact write scope

- `packages/db/fixtures/**`
- `apps/api/test/case-requirements.test.ts`
- `apps/api/scripts/case-proof.ts`

## Forbidden paths

apps/web/**; packages/ai/**; every shared/infra/API/DB path not explicitly allowed below. Allowed paths are only the explicit scope above; the task document itself is read-only to the worker. Read-only imports/inspection of dependencies allowed. Dependencies are not write permission. No real credentials in tracked files.

## Dependencies

B05, B08, B12

A `:configured` dependency means interface/tests ready; `:executed` additionally requires real GPU evidence. A task with a later live check may develop against a truthful unavailable boundary first, but cannot claim complete early.

## Relevant contracts

C01–C04/C07/C08/C12; all replenishment-v1 numeric acceptance checks

## Implementation requirements

Add deterministic inputs/counterfactual fixtures, no new calculator. Exercise each source independently: sales, current stock, eligible versus late/overdue inbound, categories/horizon, external growth, lead time, seasonality, sustained trend, lost stockout demand, isolated100× spike and same-customer split rows. Assert specified80/60/94 and spike tolerance, correct seasonal peak and stockout comparison, recurring buyer guards. Record fixture dataset hash/asOf and source contributions. Preserve six-SKU live demo and <=24-candidate cap; include at least one unsaturated borderline recurring event for GPU effect. Case-proof reads actual stored live result where provided; it does not manufacture GPU responses. If formula fails, return exact fixture to B05 owner for serialized fix.
If partner V2/1C sample arrives, report mapping mismatch to Ivan before adapting input/output; task does not invent unavailable schema or mandatory integration proof.

## Acceptance criteria

Every official numerical must-have has a passing targeted fixture and explainable counterfactual. Synthetic dataset disclosed. GPU effect still requires live B12/AI05 evidence, not these tests.

## Exact verification command/check

Run at repository root in the configured development environment unless the check says Brev. Script/file creation belongs to this task unless [execution conventions](../../execution-conventions.md) assigns its producer. Missing script is a failed/incomplete task, never a pass.

```text
pnpm --filter @atlas/api exec tsx --test test/case-requirements.test.ts
pnpm --filter @atlas/api exec tsx scripts/case-proof.ts
```

## Integration impact

Closes calculation evidence for G-MUST; read-only tests can overlap UI/AI improvements.

## Safe to delegate to a subagent

Yes, with an exclusive scope lease and the model below. Parent reviews and owns integration.

## Ready-to-run worker prompt

Read AGENTS.md, MODEL-POLICY.md, docs/execution-conventions.md and docs/backend/tasks/B09.md. Execute only B09 for Ivan. Verify dependencies including configured/executed qualifiers. Use GPT-6 Luna High. Write only: packages/db/fixtures/**; apps/api/test/case-requirements.test.ts; apps/api/scripts/case-proof.ts. Do not change contracts, shared files or another worker's files outside this scope. Implement the packet requirements, run every listed check, and return changed files, commands/results, actual-versus-configured runtime evidence and blockers. No fabricated OpenAI/GPU results, no secrets, no unrelated refactors. Do not commit/push unless the lane orchestrator explicitly assigns it. Stop with a precise mismatch if scope or contract blocks you.

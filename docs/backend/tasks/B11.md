# B11 — Clean Brev Compose reproduction and failure drill

## Goal

Clean Brev Compose reproduction and failure drill. Wave 3; estimated active effort 25 minutes (download waits may overlap other work). Follow [execution conventions](../../execution-conventions.md); commands below are implementation targets, not already-existing commands.

## Owner

Ivan — backend lane. Delegation never changes single-writer ownership.

## Recommended model

GPT-6 Sol High. Lane orchestrator: GPT-6 Sol High. Difficult bug/architecture conflict: GPT-6 Sol High; only after a serious evidenced attempt, GPT-6 Astra High. Simple bounded workers and README/tests/fixtures/small fixes: GPT-6 Luna High; normal coding workers: GPT-6 Sol Medium. No default Astra coding.

## Exact write scope

- `compose.yaml`
- `Dockerfile.api`
- `Dockerfile.web`
- `.env.example`
- `pnpm-lock.yaml`
- `infra/brev/**`
- `docs/review/reproduction.md`

## Forbidden paths

apps/web/**; packages/ai/**; every shared/infra/API/DB path not explicitly allowed below. Allowed paths are only the explicit scope above; the task document itself is read-only to the worker. Read-only imports/inspection of dependencies allowed. Dependencies are not write permission. No real credentials in tracked files.

## Dependencies

B10, AI07, FRT07

A `:configured` dependency means interface/tests ready; `:executed` additionally requires real GPU evidence. A task with a later live check may develop against a truthful unavailable boundary first, but cannot claim complete early.

## Relevant contracts

G01/G02; GPU evidence freshness; final-checklist; no public internal ports

## Implementation requirements

Rebuild from clean checkout using frozen lockfile; actual dependency versions and Docker stages, DB migrate/seed, web health/proxy and private GPU DNS. Reuse model cache while proving documented cold download steps and timing; do not imply cached startup is cold startup. Verify env examples names match all consumers, conditional registry secret stays deployment-only. Run Brev host/container GPU proof and raw smoke, then application same-run GPU proof.
Stop gpu-specialist, run an API calculation and verify degraded runtime/null report and required acknowledgment; restart via B06 script invalidating old evidence, wait readiness and verify again. No public GPU endpoint required. Save redacted command/status/time evidence, check available credits and stop only after final demo/hand-off. Never docker down -v on partner data.

## Acceptance criteria

Fresh install/build/migrate/seed/UI works; OpenAI and same-container GPU inference verified; stopped-service drill passes and recovery uses fresh evidence. Configuration alone is explicitly insufficient.

## Exact verification command/check

Run at repository root in the configured development environment unless the check says Brev. Script/file creation belongs to this task unless [execution conventions](../../execution-conventions.md) assigns its producer. Missing script is a failed/incomplete task, never a pass.

```text
pnpm install --frozen-lockfile
pnpm -r --if-present typecheck
pnpm -r --if-present build
docker compose -f compose.yaml -f infra/brev/compose.gpu.yaml build
docker compose -f compose.yaml -f infra/brev/compose.gpu.yaml up -d
bash infra/brev/verify-deployment.sh
pnpm --filter @atlas/api exec tsx scripts/combined-flow.ts
```

## Integration impact

G-RELIABLE reproduction; serialize with all root/infra writers and final B15.

## Safe to delegate to a subagent

No for the integration/ownership decision. Keep with lane orchestrator; implementation fragments require a separately bounded disjoint lease.

## Orchestrator execution prompt

Read AGENTS.md, MODEL-POLICY.md, docs/execution-conventions.md and docs/backend/tasks/B11.md. Execute only B11 for Ivan. Verify dependencies including configured/executed qualifiers. Use GPT-6 Sol High. Write only: compose.yaml; Dockerfile.api; Dockerfile.web; .env.example; pnpm-lock.yaml; infra/brev/**; docs/review/reproduction.md. Do not change contracts, shared files or another worker's files outside this scope. Implement the packet requirements, run every listed check, and return changed files, commands/results, actual-versus-configured runtime evidence and blockers. No fabricated OpenAI/GPU results, no secrets, no unrelated refactors. Do not commit/push unless the lane orchestrator explicitly assigns it. Stop with a precise mismatch if scope or contract blocks you. Keep the live gate/ownership decision with the lane orchestrator.
